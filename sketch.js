/* Alle Befehle für p5js findest du in der Dokumentation: https://p5js.org/reference/ */

function setup() {
	/* Für ältere Rechner könnt ihr hier während des Programmierens eine geringere Framerate einstellen. Damit benötigt p5js weniger Rechenleistung, zum Ausprobieren des visuellen Ergebnisses empfehle ich den Befehl wieder mittels der Zeichenfolge "//" auszukommentieren.
	
	Per default liegt die Framerate bei 60 fps */

	frameRate(24);
	
	createCanvas(400, 400);
}

function draw() {
	background(220);
}